using System;

namespace Noggolloquy.XML
{
    public class NoggolloquyXmlRegistration
    {
        //static Dictionary<Type, >

        //public static TryGetResult<object> Instantiate(XElement root)
        //{
        //    var typeGet = NoggolloquyRegistration.TryGetType(root.Name.LocalName);
        //    if (typeGet.Failed) return typeGet.BubbleFailure<object>();

        //    XAttribute baseRecordIDAttr;
        //    RecordID? baseRecordID;
        //    if (!root.TryGetAttribute("BaseRecordID", out baseRecordIDAttr)
        //        || !RecordID.TryFactory(baseRecordIDAttr.Value, out baseRecordID))
        //    {
        //        baseRecordID = null;
        //    }
        //    new object[] { baseRecordID }
        //}
    }
}
